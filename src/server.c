#include "easyTCP.h"
#include <stdio.h>

int main(void)
{
    struct socketInfo server;
    initServer(&server);
    printf("listening...\n");

    /*
    while (1)
    {
    }
    */

    return 0;
}
